const examples = [];

examples.push({
    description: 'Displays social icons',
    style: {},
    config: {},
});

examples.push({
    description: 'Displays social icons on a dark background',
    style: { backgroundColor: '#16486D' },
    config: {
        isLight: true,
    },
});

module.exports = examples;
